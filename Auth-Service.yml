spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/vikasit_ecomdb
  application:
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true 
  boot:
    admin:
      client:
        url: http://localhost:1111/ 
management:
  endpoints:
    web:
      exposure:
        include: '*'
     
